What is Hosting and Deployment ?

"Hosting" is the provision of storage space and necessary infra for a website, hosting == renting

"Deploying" refers to the process of moving the application code from a development environment
to a hosting environment that people can visit. deployment == making the rented space working

1) Hosting project to netlify ?
    -->go to netlify create an account and drag and go to create new project then select drop.
    -->Now Drop the files and boom.
    -->Netlify provides free HTTP.

2) adding Favicon to webpage, if you look from (dev tools) you can see an error message 
because browser try to load the favicon by default so lets add one,
-->get an image and name it as (favicon.ico)
-->inside html (link rel="icon" href="images/favicon.ico" type="images/x-icon)

3) git vs github, CLI VS GUI, Initializing  a git repo with git init.